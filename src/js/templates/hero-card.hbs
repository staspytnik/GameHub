<div class="hero__games--item">
  <img
    class="hero__games--img"
    src="{{background_image}}"
    alt="image"
    width="232px"
    height="173px"
  />
  <div class="hero__games--info">
    <h5 class="hero__games--title">{{name}}</h5>
    <p class="hero__games--year">{{released}}</p>
  </div>
</div>